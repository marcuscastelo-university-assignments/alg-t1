{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc-7 build active file",
            "command": "/usr/bin/gcc-7",
            "args": [
                "-g",
                "${workspaceFolder}/main.c",
                "${workspaceFolder}/aluno.c",
                "${workspaceFolder}/lista_alunos.c",
                "-o",
                "${fileDirname}/main"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}